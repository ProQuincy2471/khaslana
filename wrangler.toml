name = "khaslana"
compatibility_date = "2024-09-01"
pages_build_output_dir = "."

# Created once, by hand, per the README's deploy section:
#   wrangler kv namespace create khaslana-state
# then the id it prints replaces the placeholder below.
[[kv_namespaces]]
binding = "KHASLANA_KV"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"
